apiVersion: v2
name: docingest
description: Multi-tenant document ingestion engine for RAG and semantic search
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Application version — the DocIngest image tag deployed by default.
appVersion: "0.1.0"
home: https://github.com/tdfleming/DocIngest
sources:
  - https://github.com/tdfleming/DocIngest
keywords:
  - rag
  - embeddings
  - document-ingestion
  - vector-search
  - graphrag
maintainers:
  - name: Maetek Technologies
