name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

# Cancel superseded runs on the same ref (e.g. rapid pushes to a PR).
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install ruff
        run: python -m pip install "ruff>=0.9,<1"
      - name: ruff check
        run: ruff check src/ tests/

  backend:
    name: Backend tests (pytest)
    runs-on: ubuntu-latest
    services:
      mongodb:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --quiet --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      # Integration tests read this; defaults to localhost:27017 otherwise.
      MONGODB_URI: mongodb://localhost:27017
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install package + dev dependencies
        run: python -m pip install -e ".[dev]"
      # Entity-extraction tests use the small spaCy model (not en_core_web_lg,
      # which is only needed by the graph worker at runtime).
      - name: Download spaCy model
        run: python -m spacy download en_core_web_sm
      - name: Run tests
        run: pytest -q

  frontend:
    name: Frontend build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci
      # Runs `tsc --noEmit && vite build` — strict type-check + production build.
      - name: Build
        run: npm run build

  dockerfiles:
    name: Dockerfile lint (hadolint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Lint every Dockerfile; only hard errors fail the build (warnings such as
      # unpinned pip packages are surfaced but non-blocking for now).
      - name: Lint Dockerfiles
        run: |
          for f in docker/*.Dockerfile; do
            echo "== $f =="
            docker run --rm -i hadolint/hadolint hadolint --failure-threshold error - < "$f"
          done

  helm:
    name: Helm chart (lint + template)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
        with:
          version: v3.16.3
      - name: helm lint
        run: helm lint deploy/helm/docingest
      - name: helm template (defaults — bundled datastores)
        run: helm template docingest deploy/helm/docingest > /dev/null
      - name: helm template (external datastores + graph + ingress)
        run: |
          helm template docingest deploy/helm/docingest \
            --set mongodb.enabled=false --set qdrant.enabled=false \
            --set redis.enabled=false --set minio.enabled=false \
            --set externalDatastores.mongodbUri=mongodb://x:27017 \
            --set externalDatastores.qdrantHost=qdrant.example.net \
            --set externalDatastores.redisUrl=redis://redis.example.net:6379 \
            --set externalDatastores.minioEndpoint=s3.amazonaws.com \
            --set config.graphRagEnabled=true --set graphWorker.enabled=true \
            --set ingress.enabled=true > /dev/null
