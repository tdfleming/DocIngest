---
phase: 07-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/docingest/workers/chunker.py
  - src/docingest/db/mongodb.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "No datetime.utcnow() calls remain in codebase"
    - "Worker docstrings accurately describe their ARQ job function names"
    - "Worker logging already works (confirmed during audit — no action needed)"
  artifacts:
    - path: "src/docingest/workers/chunker.py"
      provides: "Chunk-and-embed worker with correct docstring and modern datetime"
      contains: "chunk_and_embed"
    - path: "src/docingest/db/mongodb.py"
      provides: "MongoDB operations with modern datetime usage"
      contains: "datetime.now"
  key_links: []
---

<objective>
Fix 2 minor tech debt items identified in v1 milestone audit.

Purpose: Clean codebase before milestone archival — no deprecated APIs, no misleading docs.
Output: Updated chunker.py and mongodb.py with modern datetime and accurate docstring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1-MILESTONE-AUDIT.md

@src/docingest/workers/chunker.py
@src/docingest/db/mongodb.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix docstring mismatch in chunker worker</name>
  <files>src/docingest/workers/chunker.py</files>
  <action>
    Update the module docstring (line 3) from "chunk_document" to "chunk_and_embed" to match the actual ARQ job function name.

    The function registered in WorkerSettings is `chunk_and_embed`, not `chunk_document`. The docstring is misleading.

    Only change the docstring — do not modify any logic.
  </action>
  <verify>grep -n "chunk_and_embed" src/docingest/workers/chunker.py shows docstring and function both use same name</verify>
  <done>Module docstring accurately says "chunk_and_embed"</done>
</task>

<task type="auto">
  <name>Task 2: Replace deprecated datetime.utcnow() with datetime.now(UTC)</name>
  <files>src/docingest/workers/chunker.py, src/docingest/db/mongodb.py</files>
  <action>
    Replace all `datetime.utcnow()` calls with `datetime.now(datetime.UTC)` across both files.

    Locations (6 instances):
    - chunker.py: 2 instances (lines ~142 and ~190)
    - mongodb.py: 4 instances (lines ~41, ~42, ~61, ~102)

    For each file, ensure `datetime.UTC` is available. The `datetime` module's `datetime` class has a `UTC` attribute in Python 3.11+. Import pattern: `from datetime import datetime, UTC` or use `datetime.now(datetime.UTC)` depending on existing import style.

    Check existing imports first and follow the file's pattern. If the file uses `from datetime import datetime`, add `UTC` to that import and use `datetime.now(UTC)`. If it uses `import datetime`, use `datetime.datetime.now(datetime.UTC)`.

    Do NOT change any logic — only the datetime construction method.
  </action>
  <verify>grep -rn "utcnow" src/docingest/ returns zero results</verify>
  <done>All datetime.utcnow() replaced with datetime.now(UTC) — zero deprecated calls remain</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grep -rn "utcnow" src/docingest/ returns no results
- [ ] grep -n "chunk_and_embed" src/docingest/workers/chunker.py shows match in docstring
- [ ] python -m py_compile src/docingest/workers/chunker.py succeeds
- [ ] python -m py_compile src/docingest/db/mongodb.py succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No functional changes — only docstring and datetime construction method
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-01-SUMMARY.md`
</output>
