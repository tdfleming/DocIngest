---
phase: 08-graph-data-models
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/docingest/models/graph.py
  - src/docingest/config.py
  - tests/test_graph_models.py
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-07]

must_haves:
  truths:
    - "Entity model validates name, entity_type enum, tenant_id, and sets timestamps"
    - "Relationship model validates source/target entity IDs, relation_type, and weight"
    - "Community model validates level, entity_ids list, and optional parent/child links"
    - "Config settings have correct defaults (graph_rag_enabled=False, spacy_model=en_core_web_lg, entity_confidence_threshold=0.7, max_entities_per_chunk=50)"
  artifacts:
    - path: "src/docingest/models/graph.py"
      provides: "EntityType enum, Entity, Relationship, Community Pydantic models"
      exports: ["EntityType", "Entity", "Relationship", "Community"]
    - path: "src/docingest/config.py"
      provides: "Graph RAG configuration fields on Settings class"
      contains: "graph_rag_enabled"
    - path: "tests/test_graph_models.py"
      provides: "Unit tests for all models and config fields"
      min_lines: 80
  key_links:
    - from: "src/docingest/models/graph.py"
      to: "src/docingest/models/document.py"
      via: "Same StrEnum + BaseModel + Field(alias=_id) pattern"
      pattern: "Field.*alias.*_id"
---

<objective>
Create Pydantic v2 data models for the knowledge graph (Entity, Relationship, Community) and add Graph RAG config fields, with tests written first (TDD).

Purpose: These models define the data contracts for all graph operations. Every subsequent plan depends on these types being correct and complete.
Output: `models/graph.py` with 3 models + 1 enum, updated `config.py`, passing test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-graph-data-models/08-RESEARCH.md
@src/docingest/models/document.py
@src/docingest/config.py

<interfaces>
<!-- Executor must follow existing model patterns exactly -->

From src/docingest/models/document.py:
```python
class DocumentStatus(StrEnum):
    PENDING = "pending"
    # ...

class Document(BaseModel):
    id: str = Field(default="", alias="_id")
    tenant_id: str
    # ... fields ...
    created_at: datetime = Field(default_factory=lambda: datetime.now(UTC))
    updated_at: datetime = Field(default_factory=lambda: datetime.now(UTC))

    model_config = {"populate_by_name": True}
```

From src/docingest/config.py:
```python
class Settings(BaseSettings):
    model_config = {"env_file": ".env", "extra": "ignore"}
    # fields with defaults, bare env var names
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests and models for Entity, Relationship, Community, and config</name>
  <files>tests/test_graph_models.py, src/docingest/models/graph.py, src/docingest/config.py</files>
  <behavior>
    - Entity with valid fields constructs successfully; id defaults to "", created_at/updated_at auto-set
    - EntityType enum has exactly: person, organization, location, date, event, product, concept, other
    - Entity aliases defaults to empty list, doc_ids defaults to empty list, chunk_ids defaults to empty list
    - Entity mention_count defaults to 0, embedding defaults to None, metadata defaults to empty dict
    - Entity with alias="_id" round-trips (populate_by_name works)
    - Relationship with valid fields constructs; weight defaults to 1.0
    - Relationship doc_ids and chunk_ids default to empty lists
    - Community with valid fields constructs; level is int, entity_ids defaults to empty list
    - Community parent_community_id defaults to None, child_community_ids defaults to empty list
    - Community summary_embedding defaults to None
    - Settings.graph_rag_enabled defaults to False
    - Settings.spacy_model defaults to "en_core_web_lg"
    - Settings.entity_confidence_threshold defaults to 0.7
    - Settings.max_entities_per_chunk defaults to 50
  </behavior>
  <action>
    RED: Create tests/test_graph_models.py with tests for all behaviors above. Run pytest -- tests must fail (models don't exist yet).

    GREEN: Create src/docingest/models/graph.py following the exact pattern from document.py:
    - EntityType(StrEnum) with 8 values: person, organization, location, date, event, product, concept, other
    - Entity(BaseModel): id (Field alias="_id", default=""), tenant_id (str), name (str), entity_type (EntityType), aliases (list[str] = Field(default_factory=list)), doc_ids (list[str] = Field(default_factory=list)), chunk_ids (list[str] = Field(default_factory=list)), mention_count (int = 0), embedding (list[float] | None = None), metadata (dict = Field(default_factory=dict)), created_at, updated_at. model_config = {"populate_by_name": True}
    - Relationship(BaseModel): id (alias="_id", default=""), tenant_id, source_entity_id (str), target_entity_id (str), relation_type (str), description (str = ""), weight (float = 1.0), doc_ids (list[str] = Field(default_factory=list)), chunk_ids (list[str] = Field(default_factory=list)), created_at, updated_at. model_config = {"populate_by_name": True}
    - Community(BaseModel): id (alias="_id", default=""), tenant_id, level (int), title (str = ""), summary (str = ""), entity_ids (list[str] = Field(default_factory=list)), parent_community_id (str | None = None), child_community_ids (list[str] = Field(default_factory=list)), summary_embedding (list[float] | None = None), created_at, updated_at. model_config = {"populate_by_name": True}

    Then add 4 fields to Settings class in config.py (at the end, before the closing):
    - graph_rag_enabled: bool = False
    - spacy_model: str = "en_core_web_lg"
    - entity_confidence_threshold: float = 0.7
    - max_entities_per_chunk: int = 50

    Run pytest -- all tests must pass.

    REFACTOR: Run `ruff check src/docingest/models/graph.py src/docingest/config.py tests/test_graph_models.py --fix` and verify tests still pass.
  </action>
  <verify>
    <automated>pytest tests/test_graph_models.py -x -q</automated>
  </verify>
  <done>All model tests pass. EntityType has 8 values. Entity, Relationship, Community models validate correctly with proper defaults. Config has 4 new graph fields with correct defaults. Ruff clean.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_graph_models.py -x -q` passes
- `ruff check src/docingest/models/graph.py src/docingest/config.py` reports no errors
- `python -c "from docingest.models.graph import Entity, Relationship, Community, EntityType; print('OK')"` succeeds
- `python -c "from docingest.config import settings; assert settings.graph_rag_enabled == False; print('OK')"` succeeds
</verification>

<success_criteria>
- Entity, Relationship, Community models importable and validate correctly
- EntityType StrEnum has all 8 values
- All model fields have correct types and defaults
- Config has 4 new graph_rag fields with documented defaults
- Test suite covers all behaviors listed
</success_criteria>

<output>
After completion, create `.planning/phases/08-graph-data-models/08-01-SUMMARY.md`
</output>
