---
phase: 12
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/MILESTONES.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "MILESTONES.md contains a v1.0 Graph RAG Extension block under the existing v1.0 MVP heading"
    - "The new block records phases 8-11 scope, delivered capabilities, requirements count, and audit cross-reference"
    - "No new top-level v1.1 milestone heading is created (Graph RAG is scoped into v1.0 per audit)"
    - "Existing v1.0 MVP (Shipped: 2026-03-04) block is preserved verbatim"
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "v1.0 Graph RAG Extension narrative record"
      contains: "### v1.0 Graph RAG Extension"
  key_links:
    - from: ".planning/MILESTONES.md"
      to: ".planning/v1.0-MILESTONE-AUDIT.md"
      via: "Status line cross-reference"
      pattern: "v1.0-MILESTONE-AUDIT"
---

<objective>
Append a `### v1.0 Graph RAG Extension (2026-04-12)` subsection to `.planning/MILESTONES.md` under the existing `## v1.0 MVP (Shipped: 2026-03-04)` heading. The subsection captures phases 8-11 (Graph RAG extension) within the v1.0 milestone scope, recording delivered capabilities, phase range, requirements count, current gap-closure status, and a cross-reference to the audit document.

Purpose: MILESTONES.md currently only captures the v1.0 MVP (phases 1-7). The Graph RAG extension work (phases 8-11) shipped 2026-04-12 but was never recorded. This plan closes that planning-artifact gap.

Output: Updated MILESTONES.md with one new `###` subsection inserted after the existing `## v1.0 MVP` block.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONES.md
@.planning/phases/12-graph-rag-traceability/12-CONTEXT.md
@.planning/phases/12-graph-rag-traceability/12-RESEARCH.md
@.planning/phases/12-graph-rag-traceability/12-VALIDATION.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append v1.0 Graph RAG Extension block to MILESTONES.md</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - .planning/MILESTONES.md (current state — 28 lines; existing v1.0 MVP block must remain verbatim; insertion point is after the `---` separator at line 27)
    - .planning/phases/12-graph-rag-traceability/12-CONTEXT.md (SECTION: decisions — MILESTONES.md updates (locked); SECTION: specifics — MILESTONES.md block shape with verbatim content)
    - .planning/phases/12-graph-rag-traceability/12-RESEARCH.md (SECTION: Artifact Edit Plan > MILESTONES.md — anchor line 27, content to add)
    - .planning/phases/12-graph-rag-traceability/12-VALIDATION.md (SECTION: Per-Task Verification Map — 12-03-01, 12-03-02 greps)
  </read_first>
  <action>
Use the `Edit` tool to insert the Graph RAG Extension block. Do NOT use `Write` — preserve the existing v1.0 MVP block verbatim.

**Insertion anchor:** After line 27 of MILESTONES.md (the `---` separator that closes the current v1.0 MVP block). The current file ends at line 28 (blank line).

**Content to insert (verbatim from 12-CONTEXT.md `<specifics>` block and 12-RESEARCH.md Artifact Edit Plan):**

```markdown

## v1.0 Graph RAG Extension (2026-04-12)

**Delivered:** Knowledge-graph pipeline layered onto v1.0 MVP — spaCy-based entity/relationship extraction over chunked content, MongoDB graph store with tenant isolation, and on-demand Leiden community detection with TF-IDF summaries. All functionality gated by `GRAPH_RAG_ENABLED`.

**Phases:** 8-11 (7 plans)
**Requirements declared:** 25 (GRAPH-01..07, EE-01..08, GRAPH-WORKER-01..05, COMM-01..05)
**Status:** ⚠ Gap closure in flight (phases 12-15) — see `.planning/v1.0-MILESTONE-AUDIT.md`.

**Stats:**
- graph-worker: new ARQ worker with dedicated queue (`arq:queue:graph`)
- New services: `entity_extraction`, `community_detection`
- New models: Entity, Relationship, Community
- New dependencies: spaCy (`en_core_web_lg`), python-igraph, leidenalg, scikit-learn

**Git range:** `85e5a0e` (v1.0 ship) → `479736f` (gap closure phases added)

<!-- hr -->
```

**Note on heading level:** 12-CONTEXT.md says "Add a new block under the existing `v1.0 MVP (Shipped: 2026-03-04)` heading titled `### v1.0 Graph RAG Extension (2026-04-12)`" which uses `###`. However, the existing v1.0 MVP block uses `##`. Use **`## v1.0 Graph RAG Extension (2026-04-12)`** (sibling heading, not child) because:
1. The audit (v1.0-MILESTONE-AUDIT.md) treats Graph RAG as a separate delivery within v1.0.
2. 12-VALIDATION.md grep check `grep -q '### v1.0 Graph RAG Extension' .planning/MILESTONES.md` would match `##` prefix too (grep is substring-based unless anchored), but a cleaner `##` sibling heading is more idiomatic Markdown. ADJUSTMENT: per the grep check requiring `###` specifically (12-VALIDATION.md Per-Task Verification Map row 12-03-01 uses pattern `### v1.0 Graph RAG Extension`), use **`### v1.0 Graph RAG Extension (2026-04-12)`** as a sub-heading INSIDE the v1.0 MVP block — insert it BEFORE the closing `---` separator at line 27, not after.

**Corrected insertion plan:**

Current file lines 22-28 are:
```
**Git range:** `6e60969` → `85e5a0e`

**What's next:** TBD — next milestone discussion needed

<!-- hr -->
<blank>
```

Insert the new block BETWEEN `**What's next:** TBD — next milestone discussion needed` and `---`, making Graph RAG Extension a subsection of the v1.0 MVP block. Final structure:

```
**Git range:** `6e60969` → `85e5a0e`

**What's next:** TBD — next milestone discussion needed

### v1.0 Graph RAG Extension (2026-04-12)

**Delivered:** Knowledge-graph pipeline layered onto v1.0 MVP — spaCy-based entity/relationship extraction over chunked content, MongoDB graph store with tenant isolation, and on-demand Leiden community detection with TF-IDF summaries. All functionality gated by `GRAPH_RAG_ENABLED`.

**Phases:** 8-11 (7 plans)
**Requirements declared:** 25 (GRAPH-01..07, EE-01..08, GRAPH-WORKER-01..05, COMM-01..05)
**Status:** ⚠ Gap closure in flight (phases 12-15) — see `.planning/v1.0-MILESTONE-AUDIT.md`.

**Stats:**
- graph-worker: new ARQ worker with dedicated queue (`arq:queue:graph`)
- New services: `entity_extraction`, `community_detection`
- New models: Entity, Relationship, Community
- New dependencies: spaCy (`en_core_web_lg`), python-igraph, leidenalg, scikit-learn

**Git range:** `85e5a0e` (v1.0 ship) → `479736f` (gap closure phases added)

<!-- hr -->
```

Use `###` exactly as specified (12-VALIDATION.md requires `grep -q '### v1.0 Graph RAG Extension'` to match).

**Do NOT:**
- Create a new top-level `## v1.1` or `## v2.0` heading (explicitly rejected per 12-CONTEXT.md `<deferred>` block).
- Modify the v1.0 MVP delivered bullets, stats, or git range.
- Touch any other file.

**Grep validation targets (from 12-VALIDATION.md Per-Task Verification Map):**
- `grep -q '### v1.0 Graph RAG Extension' .planning/MILESTONES.md` — row 12-03-01
- `grep -qE 'Phases?:.*8.*11' .planning/MILESTONES.md` — row 12-03-02 (matches `**Phases:** 8-11 (7 plans)`)

Per D-milestones-md (locked in 12-CONTEXT.md): one inserted block, exact wording from specifics, no new top-level milestone heading.
  </action>
  <verify>
    <automated>bash -c 'grep -q "### v1.0 Graph RAG Extension (2026-04-12)" .planning/MILESTONES.md && grep -qE "Phases?:.*8.*11" .planning/MILESTONES.md && grep -q "Knowledge-graph pipeline layered onto v1.0 MVP" .planning/MILESTONES.md && grep -q "Gap closure in flight (phases 12-15)" .planning/MILESTONES.md && grep -q "v1.0-MILESTONE-AUDIT.md" .planning/MILESTONES.md && grep -q "## v1.0 MVP (Shipped: 2026-03-04)" .planning/MILESTONES.md && ! grep -qE "^## v1\.1" .planning/MILESTONES.md && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '### v1.0 Graph RAG Extension (2026-04-12)' .planning/MILESTONES.md` exits 0 (new subsection heading present with exact date)
    - `grep -qE 'Phases?:.*8.*11' .planning/MILESTONES.md` exits 0 (phase range recorded)
    - `grep -q 'Knowledge-graph pipeline layered onto v1.0 MVP' .planning/MILESTONES.md` exits 0 (Delivered line exact match)
    - `grep -q 'Requirements declared:\*\* 25' .planning/MILESTONES.md` exits 0 (requirements count recorded)
    - `grep -q 'Gap closure in flight (phases 12-15)' .planning/MILESTONES.md` exits 0 (Status line records gap closure)
    - `grep -q '.planning/v1.0-MILESTONE-AUDIT.md' .planning/MILESTONES.md` exits 0 (audit cross-reference present)
    - `grep -q 'arq:queue:graph' .planning/MILESTONES.md` exits 0 (Stats block records ARQ queue)
    - `grep -q 'en_core_web_lg' .planning/MILESTONES.md` exits 0 (Stats block records spaCy dependency)
    - `grep -q '85e5a0e.*479736f\|85e5a0e..*479736f' .planning/MILESTONES.md` exits 0 (new block git range present)
    - `grep -q '## v1.0 MVP (Shipped: 2026-03-04)' .planning/MILESTONES.md` exits 0 (existing v1.0 MVP heading preserved)
    - `grep -q 'Multi-tenant document ingestion engine with full async pipeline' .planning/MILESTONES.md` exits 0 (existing v1.0 MVP Delivered line preserved)
    - `! grep -qE '^## v1\.1' .planning/MILESTONES.md` exits 0 (no new top-level v1.1 heading created — explicitly rejected)
    - `grep -q '6e60969' .planning/MILESTONES.md` exits 0 (original v1.0 MVP git range start preserved)
  </acceptance_criteria>
  <done>
    MILESTONES.md contains the new `### v1.0 Graph RAG Extension (2026-04-12)` subsection with all locked content (Delivered, Phases, Requirements declared, Status, Stats, Git range). The existing v1.0 MVP block is preserved verbatim. No top-level v1.1 heading was created. All 13 acceptance-criteria greps pass.
  </done>
</task>

</tasks>

<verification>
Run all acceptance-criteria greps from Task 1. Additionally verify that the existing v1.0 MVP block (lines 1-27 of the pre-edit file) is byte-identical in the post-edit file.
</verification>

<success_criteria>
- `.planning/MILESTONES.md` records the v1.0 Graph RAG Extension within the existing v1.0 milestone scope.
- Delivered capabilities, phase range, REQ-ID count, and audit link all present.
- v1.0 MVP block preserved verbatim.
- No other file modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/12-graph-rag-traceability/12-03-SUMMARY.md` following the standard SUMMARY template. Frontmatter `requirements-completed: []` (planning-artifact gap closure, no REQ-IDs claimed).
</output>
