---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GRAPH-WORKER-02, GRAPH-WORKER-05, COMM-05]
gap_closure: true

must_haves:
  truths:
    - "REQUIREMENTS.md contains all 25 Graph RAG REQ-IDs with Description, Definition of Done, and Verification criteria"
    - "GRAPH-WORKER-02, GRAPH-WORKER-05, COMM-05 are marked as Satisfied* (traceability added, VERIFICATION.md pending)"
    - "All other REQ-ID statuses are preserved unchanged from the current skeleton"
    - "Section grouping by phase (08/09/10/11) is preserved; header and coverage table remain intact"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Full Graph RAG extension traceability schema"
      contains: "Definition of Done"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/08-graph-data-models/08-01-PLAN.md"
      via: "REQ-ID declaration cross-reference"
      pattern: "GRAPH-01"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/11-community-detection/11-02-PLAN.md"
      via: "REQ-ID declaration cross-reference"
      pattern: "COMM-05"
---

<objective>
Expand `.planning/REQUIREMENTS.md` from its current 70-line skeleton into a complete traceability document containing all 25 Graph RAG extension REQ-IDs (GRAPH-01..07, EE-01..08, GRAPH-WORKER-01..05, COMM-01..05) with full Description, Definition of Done, and Verification criteria per the schema locked in 12-CONTEXT.md. Also flip the status of GRAPH-WORKER-02, GRAPH-WORKER-05, and COMM-05 from `Pending — phase 12` to `Satisfied* (traceability added, VERIFICATION.md pending)`.

Purpose: Close the "planning-artifact" gap identified in `.planning/v1.0-MILESTONE-AUDIT.md` where 25 REQ-IDs are declared only in phase PLAN frontmatter with no central traceability. This also closes the three orphaned REQ-IDs owned by phase 12.

Output: Fully-expanded REQUIREMENTS.md file (~400+ lines) with grep-verifiable schema and correct status flips.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-graph-rag-traceability/12-CONTEXT.md
@.planning/phases/12-graph-rag-traceability/12-RESEARCH.md
@.planning/phases/12-graph-rag-traceability/12-VALIDATION.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Reference shape for a single REQ-ID entry (from 12-CONTEXT.md specifics block). -->
<!-- Every one of the 25 REQ-IDs must follow this shape. -->

```markdown
- [x] **GRAPH-WORKER-02** — Worker fetches chunks via `get_doc_chunks` — Phase: 10 — Status: Satisfied* (traceability added, VERIFICATION.md pending)
  - **Description:** The graph-worker must fetch a document's chunk texts from Qdrant (not MongoDB) to run entity extraction, because chunks live in the vector store. A dedicated `get_doc_chunks(tenant_id, doc_id)` helper wraps the Qdrant scroll RPC.
  - **Definition of Done:**
    - `get_doc_chunks` exported from `src/docingest/db/qdrant.py` and imported by `src/docingest/workers/graph_builder.py`.
    - Helper returns `list[ChunkRecord]` ordered by chunk index.
    - Tenant collection existence is respected (collection may not exist → return empty list).
  - **Verification criteria:**
    - `grep -n "def get_doc_chunks" src/docingest/db/qdrant.py` returns a match.
    - `grep -n "get_doc_chunks" src/docingest/workers/graph_builder.py` returns at least one call site.
    - Running the graph-worker against a tenant with existing chunks produces `entity_count > 0` on at least one document.
```

<!-- Required checkbox syntax: `- [x]` for Satisfied/Satisfied*, `- [ ]` for Pending/Partial. -->
<!-- All 25 entries total. Do NOT invent new REQ-IDs; use only the set from the current skeleton. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite REQUIREMENTS.md with full 25 REQ-ID schema and status flips</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current 70-line skeleton — header + coverage table must be preserved)
    - .planning/phases/12-graph-rag-traceability/12-CONTEXT.md (SECTION: decisions — locked schema + status overrides; SECTION: specifics — REQ-ID coverage table and example entry shape)
    - .planning/phases/12-graph-rag-traceability/12-RESEARCH.md (SECTION: REQ-ID Inventory — paste-ready Description, DoD, and Verification criteria for all 25 REQ-IDs; this is the primary content source)
    - .planning/phases/12-graph-rag-traceability/12-VALIDATION.md (SECTION: Per-Task Verification Map — grep commands this task must satisfy)
    - .planning/v1.0-MILESTONE-AUDIT.md (SECTION: Requirements Coverage — source of truth for status categorization: satisfied* / partial / orphaned)
  </read_first>
  <action>
Use the `Write` tool (NOT `Edit`) to overwrite `.planning/REQUIREMENTS.md` completely. The new file structure:

**1. Preserve the existing header (lines 1-19 of current file):**
```markdown
# Requirements: DocIngest

**Current milestone:** v1.0 (gap closure)
**Last updated:** 2026-04-16

v1.0 MVP requirements (23 IDs, all satisfied) are archived at [.planning/milestones/v1.0-REQUIREMENTS.md](milestones/v1.0-REQUIREMENTS.md).

This file tracks the **Graph RAG extension** requirements delivered in phases 8-11 and expanded by phase 12 to include full descriptions, definitions of done, and verification criteria.

<!-- hr -->

## Coverage

- **Total REQ-IDs:** 25
- **Satisfied:** 14
- **Partial (gap closure in flight):** 7
- **Orphaned (traceability only):** 4

<!-- hr -->
```

**2. Four section headings, one per phase, keep existing order:**
- `## Graph Data Models (Phase 08)` — 7 entries: GRAPH-01 through GRAPH-07
- `## Entity Extraction (Phase 09)` — 8 entries: EE-01 through EE-08
- `## Graph Builder Worker (Phase 10)` — 5 entries: GRAPH-WORKER-01 through GRAPH-WORKER-05
- `## Community Detection (Phase 11)` — 5 entries: COMM-01 through COMM-05

**3. Each REQ-ID entry MUST follow this exact shape (from 12-CONTEXT.md decisions block — the schema is locked):**

```markdown
- [ ] **REQ-ID** — Short label — Phase: NN — Status: <status value>
  - **Description:** <1-2 sentence prose>
  - **Definition of Done:**
    - <bulleted DoD item>
    - <bulleted DoD item>
  - **Verification criteria:**
    - <grep-able or test-runnable check>
    - <grep-able or test-runnable check>
```

Checkbox syntax: use `- [x]` when status begins with `Satisfied` (including `Satisfied*`); use `- [ ]` when status is `Partial` or `Pending`. This preserves the current skeleton's checkbox convention while flipping the three status-override entries.

**4. Paste-source for each entry's Description, DoD, and Verification criteria:** `.planning/phases/12-graph-rag-traceability/12-RESEARCH.md` SECTION "REQ-ID Inventory" — each REQ-ID has a ready-to-paste block with Description, Definition of Done, Verification criteria, and Status. Copy these verbatim. The RESEARCH.md research was done by reading actual source files, so the content is already accurate.

**5. Exact status values to use (locked by 12-CONTEXT.md):**

| REQ-ID | Status |
|--------|--------|
| GRAPH-01 | Satisfied* (no VERIFICATION.md) |
| GRAPH-02 | Satisfied* (no VERIFICATION.md) |
| GRAPH-03 | Satisfied* (no VERIFICATION.md) |
| GRAPH-04 | Satisfied* (no VERIFICATION.md) |
| GRAPH-05 | Satisfied* (no VERIFICATION.md) |
| GRAPH-06 | Pending — Phase 13 (gap closure) |
| GRAPH-07 | Satisfied* (no VERIFICATION.md) |
| EE-01 | Satisfied* (no VERIFICATION.md) |
| EE-02 | Satisfied* (no VERIFICATION.md) |
| EE-03 | Satisfied* (no VERIFICATION.md) |
| EE-04 | Satisfied* (no VERIFICATION.md) |
| EE-05 | Satisfied* (no VERIFICATION.md) |
| EE-06 | Satisfied* (no VERIFICATION.md) |
| EE-07 | Satisfied* (no VERIFICATION.md) |
| EE-08 | Pending — Phase 15 (gap closure) |
| GRAPH-WORKER-01 | Pending — Phase 14 (gap closure) |
| **GRAPH-WORKER-02** | **Satisfied\* (traceability added, VERIFICATION.md pending)** ← FLIP |
| GRAPH-WORKER-03 | Pending — Phase 13 (gap closure) |
| GRAPH-WORKER-04 | Pending — Phase 14 (gap closure) |
| **GRAPH-WORKER-05** | **Satisfied\* (traceability added, VERIFICATION.md pending)** ← FLIP |
| COMM-01 | Partial — Phase 15 (fragile idx_to_entity invariant) |
| COMM-02 | Partial — Phase 15 (same fragility as COMM-01) |
| COMM-03 | Partial — Phase 15 (missing ensure_collection guard in _fetch_chunk_texts) |
| COMM-04 | Partial — Phase 15 (gap closure: asyncio deprecation) |
| **COMM-05** | **Satisfied\* (traceability added, VERIFICATION.md pending)** ← FLIP |

**6. Footer:**
- Keep the `*Satisfied*` explanation footer block (the asterisk-footnote paragraph).
- **REMOVE** the `## Notes for Phase 12` section at the bottom of the current file — it becomes obsolete once this plan executes.

**7. Rationale for decisions (per 12-CONTEXT.md):**
- COMM-04 is Partial (not Satisfied*) because it has an active asyncio.get_event_loop() anti-pattern to be fixed in phase 15. Do NOT flip it.
- The three flips (GRAPH-WORKER-02, GRAPH-WORKER-05, COMM-05) are justified because wiring is complete per integration checker — only traceability was missing.

**8. Grep validation targets (must pass after Write completes — these are the Per-Task Verification Map commands from 12-VALIDATION.md):**
- `grep -c '^- \[.\] \*\*' .planning/REQUIREMENTS.md` returns `25`
- `grep -c 'Description:' .planning/REQUIREMENTS.md` returns `25`
- `grep -c 'Definition of Done:' .planning/REQUIREMENTS.md` returns `25`
- `grep -c 'Verification criteria:' .planning/REQUIREMENTS.md` returns `25`
- `grep -E 'GRAPH-WORKER-0[25].*Satisfied\*' .planning/REQUIREMENTS.md` returns 2 matches
- `grep -E 'COMM-05.*Satisfied\*' .planning/REQUIREMENTS.md` returns 1 match

Do NOT touch source code. Do NOT touch any file other than `.planning/REQUIREMENTS.md`. This is a pure documentation edit.

Per D-schema (locked in 12-CONTEXT.md): every REQ-ID entry MUST have all six fields (Label, Description, Phase, Definition of Done, Verification criteria, Status).
Per D-status-override (locked in 12-CONTEXT.md): exactly three status flips — GRAPH-WORKER-02, GRAPH-WORKER-05, COMM-05.
  </action>
  <verify>
    <automated>bash -c 'test "$(grep -c "^- \[.\] \*\*" .planning/REQUIREMENTS.md)" = "25" && test "$(grep -c "Description:" .planning/REQUIREMENTS.md)" = "25" && test "$(grep -c "Definition of Done:" .planning/REQUIREMENTS.md)" = "25" && test "$(grep -c "Verification criteria:" .planning/REQUIREMENTS.md)" = "25" && test "$(grep -E "GRAPH-WORKER-0[25].*Satisfied\*" .planning/REQUIREMENTS.md | wc -l)" = "2" && grep -q "COMM-05.*Satisfied\*" .planning/REQUIREMENTS.md && ! grep -q "## Notes for Phase 12" .planning/REQUIREMENTS.md && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[.\] \*\*' .planning/REQUIREMENTS.md` returns `25` (all 25 REQ-ID entries present with checkbox + bold label)
    - `grep -c 'Description:' .planning/REQUIREMENTS.md` returns `25`
    - `grep -c 'Definition of Done:' .planning/REQUIREMENTS.md` returns `25`
    - `grep -c 'Verification criteria:' .planning/REQUIREMENTS.md` returns `25`
    - `grep -E 'GRAPH-WORKER-0[25].*Satisfied\*' .planning/REQUIREMENTS.md | wc -l` returns `2` (status flips applied for GRAPH-WORKER-02 and GRAPH-WORKER-05)
    - `grep -q 'COMM-05.*Satisfied\*' .planning/REQUIREMENTS.md` exits 0 (COMM-05 status flip applied)
    - `grep -q '## Graph Data Models (Phase 08)' .planning/REQUIREMENTS.md` exits 0 (phase 08 section heading preserved)
    - `grep -q '## Entity Extraction (Phase 09)' .planning/REQUIREMENTS.md` exits 0 (phase 09 section heading preserved)
    - `grep -q '## Graph Builder Worker (Phase 10)' .planning/REQUIREMENTS.md` exits 0 (phase 10 section heading preserved)
    - `grep -q '## Community Detection (Phase 11)' .planning/REQUIREMENTS.md` exits 0 (phase 11 section heading preserved)
    - `! grep -q '## Notes for Phase 12' .planning/REQUIREMENTS.md` exits 0 (obsolete footer section removed)
    - `grep -q '\*\*Last updated:\*\* 2026-04-16' .planning/REQUIREMENTS.md` exits 0 (date preserved from current file)
    - `grep -q '\*\*Total REQ-IDs:\*\* 25' .planning/REQUIREMENTS.md` exits 0 (coverage header preserved)
    - All 25 REQ-ID labels present: running the bash loop below against REQUIREMENTS.md produces no MISSING lines:
      ```bash
      for id in GRAPH-01 GRAPH-02 GRAPH-03 GRAPH-04 GRAPH-05 GRAPH-06 GRAPH-07 EE-01 EE-02 EE-03 EE-04 EE-05 EE-06 EE-07 EE-08 GRAPH-WORKER-01 GRAPH-WORKER-02 GRAPH-WORKER-03 GRAPH-WORKER-04 GRAPH-WORKER-05 COMM-01 COMM-02 COMM-03 COMM-04 COMM-05; do grep -q "\*\*$id\*\*" .planning/REQUIREMENTS.md || echo "MISSING: $id"; done
      ```
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md is fully expanded: 25 REQ-IDs each with Description, Definition of Done, and Verification criteria. The three status flips (GRAPH-WORKER-02, GRAPH-WORKER-05, COMM-05 → Satisfied*) are applied. Header, coverage table, and phase section groupings preserved. "Notes for Phase 12" footer removed. All acceptance criteria greps pass.
  </done>
</task>

</tasks>

<verification>
After this task completes, run the full Phase Gate Artifact-Presence check set from 12-RESEARCH.md's Validation Architecture block (the 25-ID loop, Description/DoD/Verification counts, status flip greps). All must pass.
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` passes all 12 acceptance-criteria greps listed in Task 1.
- No other file in the repository is modified by this plan.
- GRAPH-WORKER-02, GRAPH-WORKER-05, COMM-05 are now traceable to `Satisfied*` in REQUIREMENTS.md.
- The file is ready for a future `/gsd:verify-work` pass to produce VERIFICATION.md entries for phases 8-11 based on the grep-able DoD/Verification criteria now present.
</success_criteria>

<output>
After completion, create `.planning/phases/12-graph-rag-traceability/12-01-SUMMARY.md` following the standard SUMMARY template. Include `requirements-completed: [GRAPH-WORKER-02, GRAPH-WORKER-05, COMM-05]` in the frontmatter (these are the REQ-IDs this plan flipped to Satisfied*, closing the orphaned-traceability gap).
</output>
