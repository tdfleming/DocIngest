---
phase: 12
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "PROJECT.md no longer lists Graph RAG as out of scope"
    - "PROJECT.md Validated section records Graph RAG pipeline and feature gating as v1.0 extensions"
    - "PROJECT.md Context block notes the post-ship Graph RAG extension with audit cross-reference"
    - "PROJECT.md Last updated line shows 2026-04-16"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Project scope document aligned with shipped Graph RAG reality"
      contains: "v1.0 extension"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/v1.0-MILESTONE-AUDIT.md"
      via: "Context block cross-reference"
      pattern: "v1.0-MILESTONE-AUDIT"
---

<objective>
Update `.planning/PROJECT.md` to reflect shipped Graph RAG reality: remove the contradictory Out-of-Scope line, add two Validated entries for the Graph RAG pipeline and feature gating, extend the Context block with a post-ship Graph RAG note and audit cross-reference, and bump the Last updated line to 2026-04-16.

Purpose: PROJECT.md currently marks `Graph RAG / knowledge graph` as out of scope, contradicting the fact that phases 8-11 shipped Graph RAG in 2026-04-12. This plan aligns the project scope document with reality.

Output: Updated PROJECT.md with four targeted edits (no full rewrite).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-graph-rag-traceability/12-CONTEXT.md
@.planning/phases/12-graph-rag-traceability/12-RESEARCH.md
@.planning/phases/12-graph-rag-traceability/12-VALIDATION.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply four targeted edits to PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (current state — 77 lines; all four edit anchors must be located precisely)
    - .planning/phases/12-graph-rag-traceability/12-CONTEXT.md (SECTION: decisions — PROJECT.md updates (locked); SECTION: specifics — Wording for PROJECT.md Validated section additions)
    - .planning/phases/12-graph-rag-traceability/12-RESEARCH.md (SECTION: Artifact Edit Plan > PROJECT.md — anchor line numbers and exact strings to insert/remove)
    - .planning/phases/12-graph-rag-traceability/12-VALIDATION.md (SECTION: Per-Task Verification Map — 12-02-01, 12-02-02, 12-02-03 greps)
  </read_first>
  <action>
Use the `Edit` tool for each of the four targeted changes below. Do NOT overwrite the whole file with `Write` — preserve all other content exactly as-is.

**Edit 1 — Remove the Graph RAG out-of-scope line:**

Delete this entire line from the Out of Scope section (currently line 42 of PROJECT.md):
```
- Graph RAG / knowledge graph — research-grade complexity for marginal gains
```

The surrounding lines (OCR bullet above, RBAC bullet below) must remain unchanged.

**Edit 2 — Add two new Validated entries:**

In the `### Validated` section, insert the following two bullets at the end of the Validated list (immediately AFTER the existing final line `- ✓ Structured JSON logging with trace IDs and per-stage timing — v1.0`):

```
- ✓ Graph RAG pipeline: entity extraction (spaCy NER + SVO relationships), knowledge graph storage (MongoDB entities/relationships), and community detection (multi-resolution Leiden + TF-IDF summaries) — v1.0 extension
- ✓ Feature gating: `GRAPH_RAG_ENABLED` environment flag controls graph pipeline activation end-to-end (lifespan, chunker enqueue, worker, API) — v1.0 extension
```

These two bullets are copied verbatim from 12-CONTEXT.md `<specifics>` block → "Wording for PROJECT.md Validated section additions". The `v1.0 extension` suffix distinguishes these from the original 13 v1.0 MVP bullets.

**Edit 3 — Extend the Context block:**

In the `## Context` section, AFTER the existing line `Milestone audit passed with 100% scores across all categories.` (the last line of the current Context block, around line 50), insert this new paragraph:

```
Extended with Graph RAG pipeline post-ship (phases 8-11, 2026-04-12): entity/relationship extraction via spaCy, MongoDB graph store, Leiden community detection. See `.planning/v1.0-MILESTONE-AUDIT.md` for gap analysis.
```

The insertion preserves the existing Context content and appends a new sentence. The audit cross-reference path is exact.

**Edit 4 — Bump the Last updated line:**

Change the final line of the file from:
```
*Last updated: 2026-03-04 after v1.0 milestone*
```
to:
```
*Last updated: 2026-04-16 after v1.0 gap closure planning*
```

**Do NOT:**
- Add a Constraint or Key Decision row for Graph RAG (locked in 12-CONTEXT.md — those facts already live in CLAUDE.md and phase research docs).
- Modify the `### Active` section (still valid as "None yet — define requirements for next milestone").
- Touch any v1.0 MVP Validated bullets.
- Change the `## Core Value` statement.

**Grep validation targets (from 12-VALIDATION.md Per-Task Verification Map):**
- `! grep -q 'Graph RAG / knowledge graph' .planning/PROJECT.md` (must NOT match — Edit 1)
- `grep -q 'v1.0 extension' .planning/PROJECT.md` — Edit 2
- `grep -q '2026-04-16' .planning/PROJECT.md` — Edit 4
- `grep -q 'v1.0-MILESTONE-AUDIT.md' .planning/PROJECT.md` — Edit 3

Per D-project-md (locked in 12-CONTEXT.md): four edits, precisely. No other changes.
  </action>
  <verify>
    <automated>bash -c '! grep -q "Graph RAG / knowledge graph" .planning/PROJECT.md && test "$(grep -c "v1.0 extension" .planning/PROJECT.md)" -ge "2" && grep -q "2026-04-16 after v1.0 gap closure planning" .planning/PROJECT.md && grep -q "v1.0-MILESTONE-AUDIT.md" .planning/PROJECT.md && grep -q "entity extraction (spaCy NER" .planning/PROJECT.md && grep -q "GRAPH_RAG_ENABLED" .planning/PROJECT.md && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'Graph RAG / knowledge graph' .planning/PROJECT.md` exits 0 (Out-of-Scope line removed)
    - `grep -c 'v1.0 extension' .planning/PROJECT.md` returns `2` or more (both new Validated entries present)
    - `grep -q '2026-04-16 after v1.0 gap closure planning' .planning/PROJECT.md` exits 0 (Last updated line bumped)
    - `grep -q 'v1.0-MILESTONE-AUDIT.md' .planning/PROJECT.md` exits 0 (Context block audit cross-reference added)
    - `grep -q 'entity extraction (spaCy NER' .planning/PROJECT.md` exits 0 (Validated Graph RAG pipeline entry has exact wording)
    - `grep -q '\`GRAPH_RAG_ENABLED\`' .planning/PROJECT.md` exits 0 (Feature gating entry has exact wording with backticks)
    - `grep -q 'Extended with Graph RAG pipeline post-ship' .planning/PROJECT.md` exits 0 (Context block paragraph added)
    - `grep -q '(None yet — define requirements for next milestone)' .planning/PROJECT.md` exits 0 (Active section unchanged)
    - `grep -q 'OCR for scanned PDFs' .planning/PROJECT.md` exits 0 (adjacent Out-of-Scope bullets preserved)
    - `grep -q 'Fine-grained RBAC per document' .planning/PROJECT.md` exits 0 (adjacent Out-of-Scope bullets preserved)
    - `grep -q 'Milestone audit passed with 100% scores' .planning/PROJECT.md` exits 0 (existing Context content preserved)
    - `grep -c '^| ' .planning/PROJECT.md` returns same count as before (Key Decisions table unchanged)
  </acceptance_criteria>
  <done>
    PROJECT.md has all four edits applied: Graph RAG out-of-scope line removed, two `v1.0 extension` Validated entries added, Context block extended with post-ship Graph RAG note and audit cross-reference, Last updated line bumped to `2026-04-16 after v1.0 gap closure planning`. All other content (Key Decisions table, Constraints, v1.0 Validated bullets, Active section) unchanged. All 12 acceptance-criteria greps pass.
  </done>
</task>

</tasks>

<verification>
Run all acceptance-criteria greps from Task 1. Additionally verify that the diff against the prior version shows ONLY the four edits intended (no collateral changes): `git diff .planning/PROJECT.md | wc -l` should be small (roughly 1 line removed + 4 lines added).
</verification>

<success_criteria>
- `.planning/PROJECT.md` reflects shipped Graph RAG reality (Out-of-Scope removed, Validated extended).
- Context block links readers to the audit document.
- Last updated line shows 2026-04-16.
- No other file is modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/12-graph-rag-traceability/12-02-SUMMARY.md` following the standard SUMMARY template. Frontmatter `requirements-completed: []` (this plan closes planning-artifact gaps but claims no REQ-IDs — those are owned by 12-01).
</output>
